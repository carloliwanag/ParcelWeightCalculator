package com.carloliwanag.exam1.service;

public interface ParcelCalculatorService {
	Integer computeVolume(Integer height, Integer length, Integer width);
	Integer computeCost(Integer height, Integer length, Integer width);
}
